@model IEnumerable<GameReadOnlyVM>

@{
    ViewData["Title"] = "Games";
}

<h1>Games Played</h1>

<p>
    @if (User.IsInRole("Administrator"))
    {
        <a asp-action="Create">Create New</a>
    }
    
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CardinalsScore)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OpposingTeamScore)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OpponentName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td> 
                @Html.DisplayFor(modelItem => item.CardinalsScore)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OpposingTeamScore)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OpponentName)
            </td>
                @if (User.IsInRole("Administrator"))
                {
                    <td>
                        <a class="edit"asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                        <a class="detail"asp-action="Details" asp-route-id="@item.Id">Details</a> |
                        <a class="delete"asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                    </td>
                };
                else
                {

                <td>
                 
                    <a class="detail" asp-action="Details" asp-route-id="@item.Id">Details</a> 
              
                </td>
                }

                

               
        </tr>
}
    </tbody>
</table>
